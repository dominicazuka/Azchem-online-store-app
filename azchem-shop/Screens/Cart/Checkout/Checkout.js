import React from 'react';
import {
    Image,
    View,
    StyleSheet,
    Text,
    ScrollView,
    Button,
  } from "react-native";
import { Left, Right, Container, H1 } from "native-base";

const Checkout = () => {

    return(
        <ScrollView>
            <Text>Checkout is working</Text>
        </ScrollView>
    )
}

export default Checkout;